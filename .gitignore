node_modules
dist

# svgedit server dev data root (sticky webPort, etc.)
.svgedit-data/

# TypeScript incremental-build state file (emitted by `tsc --build`)
*.tsbuildinfo

# Coverage — keep summary for CI comparison, ignore the rest
!coverage/
coverage/*
!coverage/coverage-summary.json

# Playwright per-run artifacts (test-results/.last-run.json overwritten every run)
test-results

# Editor / IDE scratch
.vs
.vscode
.idea
.claude

# OS
.DS_Store
