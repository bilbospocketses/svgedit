# svgedit SWC Substrate — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace Vite's esbuild-based TS transform with SWC (via `unplugin-swc`) so the toolchain compiles TC39 standard decorators + `accessor` keyword into ES2022 helper-function calls, unblocking the elix → Lit migration (todo #3) on the modern decorator path.

**Architecture:** Single config change. Add two devDependencies, register one plugin in `vite.config.mjs` with inline options, document the change in CHANGELOG. No source-file modifications. Validation is "no regression on existing baseline" — full vitest + e2e + npm run build must match master's numbers.

**Tech Stack:** `unplugin-swc@^1.5` + `@swc/core@^1.15` (new), TypeScript 6.x (existing, type-check only), ESLint v9 (existing), Vite 7 (existing), Vitest 4 (existing), Playwright 1.57 (existing).

**Spec source-of-truth:** `docs/superpowers/specs/2026-05-21-svgedit-swc-substrate-design.md` (this branch, commit `50cd15c5`).

---

## Pre-flight

- [ ] **Verify spec is committed on this branch.** Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" log feat/swc-substrate --oneline -3
  ```

  Expected: top commit is `50cd15c5 docs(spec): substrate-PR for SWC TS transform (enables TC39 decorators)`, second is `7fe35d84 docs(plan): #3 PR-1 Lit infrastructure + 2 reference components (#18)` (master tip).

- [ ] **Confirm we are on feat/swc-substrate.** Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" branch --show-current
  ```

  Expected: `feat/swc-substrate`.

- [ ] **Capture vitest baseline.** Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npx vitest run 2>&1 | tail -3
  ```

  Expected: `Tests 637 passed (637)` — master baseline. If the count has drifted, update gate expectations in Task 2 to match.

- [ ] **Capture lint baseline.** Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm run lint 2>&1 | tail -5
  ```

  Expected: `0 errors, 145 warnings`. If the count has drifted, update gate expectations.

- [ ] **Capture build baseline.** Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm run build 2>&1 | tail -5
  ```

  Expected: `built in ...` + `Bundled 11 extensions`, no errors. Build succeeds on master (no decorator code exists yet that would trip Rollup).

---

## File structure

**Modified:**
- `package.json` — adds `"unplugin-swc": "^1.5.0"` + `"@swc/core": "^1.15.0"` under `devDependencies`
- `package-lock.json` — auto-regenerated by npm
- `vite.config.mjs` — adds `import swc from 'unplugin-swc'` import line, plus a new `swc.vite({...})` plugin entry at the top of the `plugins` array
- `CHANGELOG.md` — `[Unreleased]` entry

**Created:** none.

**Not modified:**
- Any `.ts` source file (substrate change only)
- Any test file (no new tests per spec § "Non-goals")
- `tsconfig.json` (target stays at `ES2025`; `tsconfigFile: false` plugin option handles SWC's reader limitation)
- `eslint.config.js` (no rule changes needed; SWC-transpiled output isn't lint-fed — eslint runs on source)
- `.gitattributes`, `.editorconfig`, or any other project config

---

## Task 1: Install SWC deps + register the Vite plugin

**Files:**
- Modify: `package.json` (npm-managed)
- Modify: `package-lock.json` (npm-managed)
- Modify: `vite.config.mjs` (manual edit)

**Context:** SWC needs both a JS wrapper (`unplugin-swc`, which adapts SWC for the Vite/Rollup ecosystem) and the underlying Rust binary (`@swc/core`). Both as devDependencies — build-time only, nothing ships to production. The Vite plugin must be registered first in the `plugins` array to intercept `.ts` files before Vite's default esbuild transform sees them.

- [ ] **Step 1: Install both devDependencies in one command**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm install -D unplugin-swc @swc/core
  ```

  Expected: npm reports `added N packages` (N ≈ 285 from probe; exact may differ), `0 vulnerabilities`. Two new entries in `package.json` under `devDependencies`.

- [ ] **Step 2: Inspect package.json diff**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" diff package.json
  ```

  Expected: two new lines under `devDependencies`:

  ```
  +    "@swc/core": "^1.15.33",
  +    "unplugin-swc": "^1.5.9",
  ```

  Exact patch versions may differ — npm pins what's current at install time. If the major versions differ (e.g., `^2` for unplugin-swc), halt and surface — spec assumed `^1.5`.

- [ ] **Step 3: Edit vite.config.mjs — add the import line**

  Open `C:/Users/jscha/source/repos/svgedit/vite.config.mjs`. Add a new import line BETWEEN `import string from 'vite-plugin-string'` and the blank line that precedes `const editorEntries`:

  ```js
  import { resolve } from 'node:path'
  import { defineConfig } from 'vite'
  import dynamicImportVars from '@rollup/plugin-dynamic-import-vars'
  import string from 'vite-plugin-string'
  import swc from 'unplugin-swc'
  ```

  The new `import swc from 'unplugin-swc'` is the only line added in this step.

- [ ] **Step 4: Edit vite.config.mjs — register the plugin first in plugins array**

  In the same file, find the `plugins: [` opening (around line 36 on master). Add the SWC plugin as the FIRST entry, before the existing `{ name: 'svgedit-skip-vite-build-html', ...}` block:

  ```js
    plugins: [
      swc.vite({
        tsconfigFile: false,
        jsc: {
          parser: { syntax: 'typescript', decorators: true },
          transform: { decoratorVersion: '2022-03' },
          target: 'es2022',
          keepClassNames: true
        },
        sourceMaps: true
      }),
      {
        name: 'svgedit-skip-vite-build-html',
        apply: 'build',
        // ...rest unchanged
  ```

  No other changes to `vite.config.mjs` in this step.

- [ ] **Step 5: Verify the full diff before continuing**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" diff vite.config.mjs
  ```

  Expected: exactly one new import line (`import swc from 'unplugin-swc'`) and one new plugin entry (the 10-line `swc.vite({...})` block). No other modifications. If anything else changed (whitespace shifts, line reorders, etc.), revert those — the diff should be minimal and surgical.

- [ ] **Step 6: Verify tsc still passes**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npx tsc --build --force 2>&1 | tail -10
  ```

  Expected: zero output (clean compile). 0 errors.

  Note: tsc never sees the SWC config. This step confirms the type-check path is unchanged.

- [ ] **Step 7: Verify lint still passes at baseline**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm run lint 2>&1 | tail -5
  ```

  Expected: `0 errors, 145 warnings` (matches Pre-flight baseline).

  Note: ESLint runs on source `.ts` files, not on SWC's transpiled output. The substrate change doesn't affect lint surface.

- [ ] **Step 8: Verify vitest still at 637/637**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npx vitest run 2>&1 | tail -3
  ```

  Expected: `Tests 637 passed (637)`.

  This step exercises the SWC plugin for the first time (vitest uses Vite's transform pipeline). If SWC misconfigures and tests fail, this is where it surfaces.

- [ ] **Step 9: Verify npm run build succeeds**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm run build 2>&1 | tail -5
  ```

  Expected: `✓ built in ...ms` + `Bundled 11 extensions`. Rollup uses the SWC-transpiled output. If SWC's config is wrong for the build path, this fails.

- [ ] **Step 10: Commit**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" add package.json package-lock.json vite.config.mjs
  git -C "C:/Users/jscha/source/repos/svgedit" commit -m "feat(substrate): SWC for TS transform via unplugin-swc

Adds unplugin-swc + @swc/core as devDependencies and registers swc.vite() as
the first Vite plugin. Inline options enable TC39 standard decorators with
decoratorVersion 2022-03, target es2022, and tsconfigFile:false (SWC's
tsconfig reader does not recognize target ES2025; tsc continues to use the
project tsconfig unchanged).

Substrate change only — no source files touched, no behavior change on the
existing baseline. Validation: tsc 0 / lint 145w / vitest 637 / npm run build
success (matches master)."
  ```

  Expected: single commit on `feat/swc-substrate`.

---

## Task 2: Full validation gate (regression check)

**Files:** none modified.

**Context:** Validates that the substrate change has zero regression on the existing baseline. Each gate must match master's numbers exactly.

- [ ] **Step 1: Run full vitest suite**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npx vitest run 2>&1 | tail -5
  ```

  Expected: `Tests 637 passed (637)`. (Task 1 Step 8 already verified this; this step is the final pre-PR check.)

- [ ] **Step 2: Run e2e suite across chromium + firefox**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npx tsx scripts/run-e2e.ts 2>&1 | tail -10
  ```

  Expected: `250 passed` across both browsers (~2-3 min runtime). No new tests; this is the master baseline.

  If any e2e test fails, capture which one and halt — SWC's emit may have subtly broken something the unit tests didn't catch.

- [ ] **Step 3: Verify npm run build is clean**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm run build 2>&1 | tail -5
  ```

  Expected: `✓ built in ...ms` + `Bundled 11 extensions`. (Task 1 Step 9 already verified; this step locks the final build artifact for PR review.)

- [ ] **Step 4: No commit for this task** — validation only. If any gate fails, halt and diagnose before continuing to Task 3.

---

## Task 3: Manual browser smoke (optional — request user OK before browser launch)

**Files:** none modified.

**Context:** The probe at `%TEMP%/svgedit-swc-probe` already validated end-to-end browser rendering with real Lit components. Substrate-PR has no Lit code, so the manual smoke here is "does the editor still load identically to master?" — paranoid sanity check, not strictly required.

**IMPORTANT:** Launching Playwright spawns Chrome on the user's machine (this MCP attaches to system Chrome, not a separate bundled Chromium). Surface that explicitly before running browser tools. The user previously requested awareness but not gating.

- [ ] **Step 1: Ask user whether to perform manual smoke**

  Pose: "Substrate validation gates (vitest + e2e + build) are all green. Want me to also run a manual browser smoke (will pop up Chrome) to confirm the editor renders identically to master? Or skip and proceed to CHANGELOG?"

  If the user says skip, jump to Task 4.

- [ ] **Step 2: Start Vite dev server in background**

  Run (`run_in_background: true`):

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && npm start
  ```

  Wait for `VITE vX.X.X ready` in the output. Use an until-loop:

  ```
  until grep -q "VITE.*ready" <output-file>; do sleep 1; done; echo "READY"
  ```

- [ ] **Step 3: Signal browser launch + navigate**

  Tell the user: "About to launch Chrome via Playwright — your Chrome will pop up. Heads-up."

  Then call:

  ```
  mcp__plugin_playwright_playwright__browser_navigate({ url: 'http://localhost:8000/src/editor/index.html' })
  mcp__plugin_playwright_playwright__browser_wait_for({ time: 4 })
  ```

  Expected: page loads with 0 console errors.

- [ ] **Step 4: Take a screenshot for the PR description**

  Call:

  ```
  mcp__plugin_playwright_playwright__browser_take_screenshot({ type: 'png', filename: 'svgedit-substrate-smoke.png', fullPage: false })
  ```

  Save the screenshot path for inclusion in the PR body.

- [ ] **Step 5: Close browser + stop dev server**

  Call:

  ```
  mcp__plugin_playwright_playwright__browser_close()
  ```

  Then stop the dev server via `TaskStop`:

  ```
  TaskStop({ task_id: <vite-dev-task-id> })
  ```

  Verify port 8000 freed:

  ```
  PowerShell: $p = (Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue).OwningProcess; if ($p) { Stop-Process -Id $p -Force }
  ```

- [ ] **Step 6: No commit for this task** — smoke is verification only.

---

## Task 4: CHANGELOG entry + commit

**Files:**
- Modify: `CHANGELOG.md`

**Context:** Substrate change deserves a dedicated CHANGELOG entry so future readers (PR-1/PR-2/PR-3 agents, code reviewers post-merge) see the WHY and recipe. Use the existing `## [Unreleased]` heading; add the new entry as the topmost item.

- [ ] **Step 1: Read the current top of CHANGELOG.md**

  Run:

  ```
  head -30 "C:/Users/jscha/source/repos/svgedit/CHANGELOG.md"
  ```

  Confirm the `## [Unreleased]` heading exists. If not, halt — the CHANGELOG structure may have drifted.

- [ ] **Step 2: Add the entry at the top of `[Unreleased]`**

  Edit `CHANGELOG.md`. Under `## [Unreleased]` (above any existing entries inside that section), insert:

  ```markdown
  ### Substrate (SWC for TS transform — YYYY-MM-DD)

  Switch the TS-transform layer of Vite's pipeline from esbuild to SWC via `unplugin-swc`. Required to support TC39 standard decorators + the `accessor` keyword for the upcoming elix → Lit migration (todo #3, PR-1 onwards). esbuild's TC39 decorator support is incomplete — it passes the raw syntax through, leaving the browser to parse it. Rollup's parser hard-rejects the `accessor` keyword. SWC compiles stage-3 decorators to ES2022 helper-function calls that every modern browser parses cleanly.

  - Adds `unplugin-swc@^1.5` + `@swc/core@^1.15` as `devDependencies`.
  - `vite.config.mjs` registers `swc.vite({ jsc: { parser: { decorators: true }, transform: { decoratorVersion: '2022-03' }, target: 'es2022', keepClassNames: true }, tsconfigFile: false, sourceMaps: true })` as the first plugin in the array. Inline options only — no `.swcrc` (single source of truth).
  - `tsconfigFile: false` works around SWC's tsconfig reader not recognizing `target: "ES2025"`. tsc continues type-checking against ES2025 unchanged.
  - Verification: tsc 0 errors / lint 0e/145w / vitest 637/637 / `npm run build` success (25 modules, 11 extensions bundled) / e2e 250/250 across chromium + firefox / no source files modified.
  - Spec: `docs/superpowers/specs/2026-05-21-svgedit-swc-substrate-design.md`. Probe artifacts (not committed) at `%TEMP%/svgedit-swc-probe` validated end-to-end with real Lit components.
  - Unblocks: PR-1 of the elix → Lit migration (todo #3) and every subsequent per-component conversion.
  ```

  Replace `YYYY-MM-DD` with the execution date (use today's date in ISO format).

- [ ] **Step 3: Verify the diff**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" diff CHANGELOG.md
  ```

  Expected: pure addition (no lines removed), the new entry sits at the top of `## [Unreleased]`, no other content disturbed.

- [ ] **Step 4: Commit**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" add CHANGELOG.md
  git -C "C:/Users/jscha/source/repos/svgedit" commit -m "docs(changelog): document SWC substrate change (enables TC39 decorators)"
  ```

---

## Task 5: Push branch + open PR + watch CI + squash-merge

**Files:** none modified locally.

**Context:** Standalone PR per spec § "PR shape + sequencing". Use the signed-repo squash-merge SOP from CLAUDE.md — `gh pr merge --squash --delete-branch`, never `--rebase`.

- [ ] **Step 1: Push the branch to remote**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" push -u origin feat/swc-substrate
  ```

  Expected: branch created on remote; upstream tracking set. Output mentions "new branch" and "set upstream".

- [ ] **Step 2: Open the PR with a verbatim body**

  Run (the body uses a single-quoted heredoc so PowerShell doesn't expand `$` or backticks; the closing `EOF` is at column 0):

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && gh pr create --title "feat(substrate): SWC for TS transform (enables TC39 decorators + auto-accessor)" --body "$(cat <<'EOF'
  ## Summary

  Switch the TS-transform layer of Vite's pipeline from esbuild to SWC via `unplugin-swc`. Required substrate for the elix → Lit migration (todo #3, PR-1 onwards) to use TC39 standard decorators + the `accessor` keyword. esbuild's TC39 decorator support is incomplete (passes raw syntax through); Rollup's parser hard-rejects the `accessor` keyword. SWC compiles stage-3 decorators to ES2022 helper-function calls that every modern browser parses cleanly.

  - Adds `unplugin-swc@^1.5` + `@swc/core@^1.15` as devDependencies.
  - `vite.config.mjs` registers the SWC plugin first in the plugins array with inline options (no external `.swcrc`).
  - `tsconfigFile: false` works around SWC not recognizing tsconfig's `target: "ES2025"`. tsc continues using ES2025 unchanged.
  - No source files modified. No tests added. No tsconfig changes.

  ## Why now / why not before

  The elix → Lit migration locked TC39 standard decorators (no \`experimentalDecorators\`) at design time. PR-1 of that migration (currently on \`feat/pr-1-lit-infra-and-references\`, paused) hit this substrate gap mid-execution: \`tsc\` accepted the decorator code, but \`esbuild\` and \`Rollup\` did not. A probe worktree at \`%TEMP%/svgedit-swc-probe\` validated SWC as the fix end-to-end (real Lit components in real Chrome). This PR brings the substrate to master so PR-1 can resume from a clean foundation.

  ## Spec

  \`docs/superpowers/specs/2026-05-21-svgedit-swc-substrate-design.md\` (this branch). Includes pipeline-before-vs-after diagrams, field-by-field SWC config rationale, risks, and PR sequencing for the follow-on PR-1.

  ## Test plan

  - [x] \`npx tsc --build --force\` → 0 errors
  - [x] \`npm run lint\` → 0 errors / 145 warnings (baseline maintained)
  - [x] \`npx vitest run\` → 637/637 passing (baseline maintained)
  - [x] \`npm run build\` → success, 25 modules, 11 extensions
  - [x] \`npx tsx scripts/run-e2e.ts\` → 250/250 passing across chromium + firefox
  - [ ] All 4 required CI checks green (\`build-and-test\`, \`Analyze (javascript-typescript)\`, \`Analyze (actions)\`, \`Scorecard analysis\`)
  - [ ] Merge via \`gh pr merge --squash --delete-branch\` per signed-repo squash SOP

  ## What's next after merge

  Probe cleanup (\`git worktree remove %TEMP%/svgedit-swc-probe\`), then rebase \`feat/pr-1-lit-infra-and-references\` onto the new master tip (expected mechanical conflict in \`package.json\`: substrate-PR adds devDeps, PR-1's first commit adds a runtime dep \`lit\`). PR-1 execution resumes at Task 5 (Vite HMR substrate smoke), which is no longer blocked.
  EOF
  )"
  ```

  Expected: gh prints the new PR URL (e.g., `https://github.com/bilbospocketses/svgedit/pull/19`). Save the PR number for Step 3.

- [ ] **Step 3: Watch CI checks**

  Run (foreground OK since it's ~4 min):

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && gh pr checks <PR-NUMBER> --watch
  ```

  Expected: all 4 required checks pass within ~5 min total:

  - `build-and-test` (~4 min) — verifies SWC works in CI
  - `Analyze (javascript-typescript)` (~1 min) — CodeQL on source
  - `Analyze (actions)` (~45 s) — CodeQL on workflows
  - `Scorecard analysis` (~20 s) — supply-chain checks

  If `build-and-test` fails, the SWC config may behave differently in CI (e.g., due to platform-specific `@swc/core-{platform}-{arch}` binary not being pinned in package-lock). Halt + investigate.

- [ ] **Step 4: Merge once CI greens**

  Run:

  ```
  cd "C:/Users/jscha/source/repos/svgedit" && gh pr merge <PR-NUMBER> --squash --delete-branch
  ```

  Expected: web-flow-signed squash merge lands on master. Branch deleted from remote AND local automatically.

- [ ] **Step 5: Sync local master**

  Run:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" checkout master
  git -C "C:/Users/jscha/source/repos/svgedit" pull --ff-only origin master
  git -C "C:/Users/jscha/source/repos/svgedit" log --oneline -3
  ```

  Expected: the new substrate-PR squash commit at the top, signed by GitHub's web-flow key. Below it: `7fe35d84` (PR #18 plan) and `83e70977` (PR #17 spec).

---

## Post-PR wrap-up

- [ ] **Probe cleanup.** Remove the throwaway worktree from disk:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" worktree remove "C:/Users/jscha/AppData/Local/Temp/svgedit-swc-probe"
  ```

  Expected: worktree removed, branch tracking cleaned, no errors. If `worktree remove` complains about uncommitted changes (the probe added `__probe.ts`, copied Lit files, edited config), use `--force`:

  ```
  git -C "C:/Users/jscha/source/repos/svgedit" worktree remove --force "C:/Users/jscha/AppData/Local/Temp/svgedit-swc-probe"
  ```

- [ ] **Update PR-1 plan doc** (the one on `feat/pr-1-lit-infra-and-references` branch) with a one-line Pre-flight addition: "Confirm SWC substrate is on master via `git log master --oneline -5 | grep substrate`." This is a marginal touch-up; consider rolling it into the PR-1 rebase commit rather than a standalone commit.

- [ ] **Defer PR-1 rebase** to a fresh session if user prefers — that work involves cherry-picking the existing 8 commits onto the substrate-PR'd master, mechanically resolving the `package.json` conflict, then resuming PR-1 execution. Not in scope for this substrate-PR.

---

## Self-review checklist (run BEFORE execution)

1. **Spec coverage** — every spec section has a task:
   - ✓ Spec § Goals → Task 1 implements the substrate; Task 2 verifies no regression
   - ✓ Spec § Architecture / Pipeline overview → Task 1 Steps 3-4
   - ✓ Spec § Architecture / Configuration → Task 1 Step 4 with verbatim config block
   - ✓ Spec § Architecture / Plugin order → Task 1 Step 4 (registered FIRST)
   - ✓ Spec § Validation gate → Task 1 Steps 6-9 + Task 2
   - ✓ Spec § PR shape + sequencing → Task 5
   - ✓ Spec § Risks (1-5) → mitigations all covered by gate runs + CI checks
   - ✓ Spec § Non-goals: no Lit code (correct — none added); no tsconfig change (correct — none); no smoke test (correct — only existing tests); no README rewrite (correct — none)
   - ✓ Post-PR wrap-up covered by the dedicated section

2. **Placeholder scan** — no "TBD" / "TODO" / "implement later" / "add appropriate error handling" / "similar to Task N" anywhere. Verified.

3. **Type consistency** — config field names match across spec and plan:
   - `tsconfigFile: false` (both)
   - `jsc.parser.syntax: 'typescript'` (both)
   - `jsc.parser.decorators: true` (both)
   - `jsc.transform.decoratorVersion: '2022-03'` (both)
   - `jsc.target: 'es2022'` (both)
   - `jsc.keepClassNames: true` (both)
   - `sourceMaps: true` (both)

4. **Branch + PR flow matches signed-repo squash SOP** — Task 5 uses `gh pr merge --squash --delete-branch`. ✓

5. **Pre-flight checks valid** — spec commit `50cd15c5` is on `feat/swc-substrate` (verified at plan authorship). ✓

6. **Manual smoke is optional + gated on user OK** — Task 3 Step 1 asks before launching Chrome, addresses the lesson from earlier in the session. ✓

7. **Probe cleanup is in the post-PR wrap-up** — not in the substrate-PR's commits. ✓
